# Book

Book is a very simple page.
It has a description explaining that booking occurs on an external site.
There is a link that says "Book Now".
The link opens the user in a new tab.
The destination URL is defined in the /content/book.json config